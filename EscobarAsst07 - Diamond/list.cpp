#include <iostream>
#include <string>
using namespace std;
/*
// Function
void closing(int iCount, char cLetter) { 
	int i,j,k;

// For loop that place the increasing spaces from the bottom middle point.
for( i = (iCount / iCount); i <= (iCount /2) - 1; i++ ) {
		for( j = 0; j < i; j++ ) {
			cout << ".";
		}
 cout << cLetter;
 // For Loop that place the decreasing spaces between characteres.
		for(k = iCount-3; k >= j*2; k-- )	{
			cout << ".";
		}
 cout << cLetter << endl;
		}
		
}
 */